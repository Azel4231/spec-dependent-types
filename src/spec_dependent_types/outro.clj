(ns spec-dependent-types.outro)

;; Conclusion

;; -> dependent types useful for checksum-based data
;; -> s/and passes conformed data on to the successive specs
;; -> specs "encapsulate" knowledge about order (but not the data itself)
;; -> maintain access to raw data

